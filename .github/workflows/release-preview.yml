name: Release Preview

on:
  pull_request:
    branches: [dev]
  workflow_dispatch:

jobs:
  release_preview:
    uses: OpenVoiceOS/gh-automations/.github/workflows/release-preview.yml@dev
    secrets: inherit
    with:
      package_name: 'ovos_wolfram_alpha_plugin'
      version_file: 'ovos_wolfram_alpha_plugin/version.py'
